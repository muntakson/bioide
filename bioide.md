# BioIDE — 개요와 인포그래픽 생성 프롬프트

*(Overview of BioIDE + three NotebookLM infographic prompts for the landing page)*

이 문서는 랜딩 페이지에 쓸 **BioIDE 소개**와, NotebookLM에서 **scRNA-seq 교육 인포그래픽 3종**을
생성하기 위한 프롬프트를 담습니다. 세 인포그래픽은 각각 (1) 암세포에서 유전자 발현 데이터가
어떻게 만들어지는가, (2) scRNA-seq 데이터 분석 워크플로, (3) count matrix(발현 행렬)입니다.

---

## 1. BioIDE란 무엇인가 (What BioIDE does)

**BioIDE** (GHBIO Co-Scientist)는 생물학자를 위한 **브라우저 기반 생명정보 워크벤치**입니다.
PlatformIO가 임베디드 개발을 한 화면에서 통합하듯, BioIDE는 **암 + 단일세포 RNA-seq(scRNA-seq)
논문 재현**에 필요한 모든 것 — 데이터 다운로드, GPU 분석 파이프라인, AI 해석, 리포트·논문 작성 —
을 하나의 IDE(code-server) 안에서 제공합니다.

| | |
|---|---|
| **정체성** | 생물학자용 생명정보 워크벤치 (VS Code / code-server 확장, 브랜드명 BioIDE) |
| **대상** | Copilot 없이 분석을 배우려는 한국 생물학자 · 대학원생 |
| **핵심 UX** | GHBIO Home → 튜토리얼 Dashboard → 원클릭 파이프라인 실행 → AI 분석 → 리포트 |
| **분석 스택** | Python(Scanpy) · GPU(scVI · rapids-singlecell · PyTorch) · AI 해석 |
| **언어** | UI·AI 모두 한국어 우선 (bilingual) |
| **배포** | 브라우저에서 바로 사용 (rna.bioide.org) |

### BioIDE가 하는 일 (한 줄 요약)

> **기념비적 암 scRNA-seq 논문**을, 저자의 라벨을 소비하지 않고 **우리가 새로 짠 GPU-Python
> 코드로 처음부터 다시 도출**해 저자의 주장을 **독립적으로 검증**한다 — *Reproduce, don't consume.*

### 주요 기능

- **원클릭 재현 파이프라인** — 논문별 파이프라인을 Dashboard에서 "전체 분석 실행" 한 번으로
  데이터 다운로드 → QC → 클러스터링 → 세포유형 주석 → 악성세포 식별까지 실행. 모든 단계는
  멱등(idempotent)이라 중단해도 이어서 재개됩니다.
- **독립 검증(Independent validation)** — 우리 결과를 저자 주장과 정량 대조(세포유형 일치,
  악성 F1, CNV 농도 등)해 **일치 / 부분일치 / 불일치**를 정직하게 판정합니다.
- **AI 분석 패널** — 결과 CSV를 근거로 한 번의 스트리밍 응답으로 해석을 제공(에이전트·툴 루프
  없는 신뢰성 우선 설계).
- **리포트·논문 생성** — 결과를 고등학생용 쉬운 리포트, 교육 논문, 재현 연구 논문(PDF)으로 변환.
- **파이프라인 만들기(Create pipeline)** — 공개 데이터가 있는 암 scRNA-seq 논문을 AI로 찾아
  GPU-Python 재현 계획 초안을 설계. (초안이지 자동 배포가 아님 — 사람이 검증 후 승격.)
- **검증된 재현 실적** — 랜딩의 "Verified Reproductions"에는 실제 검증 실행에서 AGREE 판정을
  받은 논문만 등재됩니다 (Tirosh 2016, Puram 2017, Peng 2019, Kim 2020 등).

---

## 2. NotebookLM 인포그래픽 생성 프롬프트 3종

> **사용법:** NotebookLM에서 관련 소스(교과서·리뷰 논문·본 프로젝트 문서)를 추가한 뒤,
> Studio 패널의 **인포그래픽(Infographic)** 생성 기능에 아래 프롬프트를 붙여넣으세요.
> 각 프롬프트는 자체 완결형이라 소스가 부족해도 표준 지식으로 그릴 수 있게 구성했습니다.
> 브랜드 톤: **한국어 우선, 깔끔한 과학 다이어그램, 파랑–청록 계열, 세포 그림 최소화·화살표 흐름 강조.**

---

### 프롬프트 ① — "암세포 → 유전자 발현 데이터" (Wet-lab 생성 과정)

```
scRNA-seq 실험이 수많은 암세포로부터 유전자 발현 데이터를 어떻게 만들어내는지 보여주는
세로형 교육 인포그래픽을 한국어로 만들어줘. 대상은 분석을 처음 배우는 생물학자·대학원생.

왼쪽에서 오른쪽으로 흐르는 5단계 파이프라인을 큰 화살표로 연결해서 그려줘:

1. 종양 조직 채취 → 단일세포 분리
   (고형 종양을 효소로 해리(dissociation)해 낱개 세포 현탁액을 만든다. 암세포 + 면역세포 +
    섬유아세포 등 다양한 세포가 섞여 있음을 강조.)
2. 미세유체 액적 캡슐화 (10x Genomics droplet)
   (각 세포를 기름방울 속에서 고유 바코드가 붙은 젤 비드 하나와 짝지음 → "1세포 = 1바코드".)
3. 세포 바코드(Cell Barcode) + UMI 태깅 & 역전사
   (세포 바코드 = 어느 세포에서 왔는지, UMI = 원본 mRNA 분자 하나를 식별해 PCR 중복 제거.
    mRNA를 cDNA로 역전사.)
4. 라이브러리 제작 → 차세대 시퀀싱(NGS)
   (수백만 개의 짧은 리드(read)를 대량 병렬로 읽음.)
5. 리드 정렬(alignment) → 유전자별 카운트 집계
   (리드를 유전자 바코드/UMI로 되풀어 "세포 × 유전자" 카운트를 채운다.)

핵심 개념 3개를 아이콘 배지로 강조: Cell Barcode(세포 식별), UMI(분자 식별·중복 제거),
Droplet(1세포 격리). 마지막에 "수천~수십만 개의 개별 암세포를 동시에 측정한다"는 문구를 크게.
색은 파랑–청록, 화살표 흐름 강조, 세포는 단순한 동그라미로. 텍스트 라벨은 한국어(영문 병기).
```

---

### 프롬프트 ② — "scRNA-seq 데이터 분석 워크플로"

```
count matrix(발현 행렬)에서 시작해 생물학적 결론에 이르는 표준 scRNA-seq 분석 워크플로를
보여주는 세로형 흐름도 인포그래픽을 한국어로 만들어줘. 대상은 대학원 수준 학습자.

위에서 아래로 이어지는 단계별 흐름도로, 각 박스에 (무엇을·왜)를 한 줄씩 적어줘:

1. 품질관리(QC) — 죽은 세포·이중체(doublet)·빈 액적 제거 (유전자 수·미토콘드리아 비율 필터)
2. 정규화 — normalize_total → log1p (세포별 시퀀싱 깊이 차이 보정)
3. 고변동 유전자(HVG) 선택 — 정보량 큰 유전자만 남김
4. 차원 축소 — PCA (수천 유전자 → 수십 주성분)
5. 이웃 그래프 & 클러스터링 — kNN 그래프 → Leiden 클러스터 (비슷한 세포끼리 묶음)
6. 시각화 — UMAP 임베딩 (2D 지도; "거리는 정량이 아님" 주의 문구)
7. 세포유형 주석 — marker 유전자로 각 클러스터에 세포유형 부여
8. 악성세포 식별 & 차등발현(DE) — CNV 추론 등으로 종양세포 판정, 프로그램 도출
9. 생물학적 해석 — 종양 미세환경·조성 비교, 논문 주장과 대조

오른쪽에 작은 사이드바로 "현대 스택" 배지: Scanpy · GPU(scVI, rapids-singlecell) · AI 해석.
하단에 BioIDE 원칙 한 줄: "저자 라벨을 소비하지 말고 처음부터 다시 도출해 검증한다."
색은 파랑–청록 계열, 단계마다 번호 원형 배지, 화살표로 순서 강조. 라벨은 한국어(영문 병기).
```

---

### 프롬프트 ③ — "Count Matrix(발현 행렬)란 무엇인가"

```
scRNA-seq의 핵심 산출물인 count matrix(발현 행렬)의 구조와 특징을 설명하는 교육
인포그래픽을 한국어로 만들어줘. 대상은 행렬을 처음 접하는 생물학자.

중앙에 큰 표(그리드)를 그려줘:
- 행(row) = 세포(Cell) — 각 행은 개별 암세포 하나 (예: Cell_1 … Cell_N, N은 수천~수십만)
- 열(column) = 유전자(Gene) — 각 열은 하나의 유전자 (예: EPCAM, CD8A, MKI67 …)
- 각 칸(cell value) = 그 세포에서 그 유전자의 mRNA 카운트(정수, UMI 개수)

표 옆에 콜아웃(말풍선)으로 핵심 특징 4개를 강조:
1. 크기 — "세포 수 × 유전자 수" (예: 50,000 세포 × 20,000 유전자 = 매우 큼)
2. 희소성(Sparsity) — 대부분의 칸이 0 (dropout; 한 세포에서 검출되는 유전자는 일부뿐)
3. 정수 카운트 — 값은 측정된 mRNA 분자(UMI) 개수
4. 저장 형식 — 희소 행렬로 압축 저장 (예: 10x MTX, .h5ad/AnnData)

아래쪽에 작은 화살표로 "이 행렬이 모든 다운스트림 분석(QC → 클러스터링 → 세포유형)의
출발점"임을 표시. 예시 표는 EPCAM 열에서 상피/암세포는 높은 값, 면역세포는 0으로 보여
"발현 패턴이 세포 정체성을 드러낸다"는 직관을 주게 그려줘.
색은 파랑–청록, 0인 칸은 연한 회색·발현 있는 칸은 진한 색으로 히트맵처럼. 라벨은 한국어(영문 병기).
```

---

## 3. 세 인포그래픽이 이어지는 이야기

세 장은 **하나의 흐름**으로 읽히도록 설계했습니다:

**① 암세포(wet-lab)** 로 발현 데이터를 *만들고* → **③ count matrix** 로 *구조화하고* →
**② 분석 워크플로** 로 *해석*합니다. 랜딩 페이지에서는 이 순서(①→③→②)로 배치하면
"실험 → 데이터 → 분석"의 자연스러운 스토리라인이 됩니다.
