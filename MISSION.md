# 📜 BioIDE 헌장 (Central Dogma · Constitution)

> BioIDE로 만드는 **모든 재현 튜토리얼**이 반드시 지켜야 하는 헌법입니다.
> "파이프라인 만들기"(Create pipeline) 패널에 상시 노출되고, 튜토리얼을 설계·생성하는
> **모든 AI 단계의 시스템 프롬프트에 자동으로 주입**됩니다. 단일 원본은 `src/mission.ts`
> 입니다 — 문구를 바꾸려면 그 파일을 고치세요(이 문서는 사람이 읽는 사본).

## 사명 (Mission)

암(cancer) + 단일세포 RNA-seq(scRNA-seq) 분야의 **기념비적 논문**을, **우리가 독립적으로
새로 작성한 GPU 중심 Python 코드**로 재현해 저자의 주장을 검증하고, 그 과정에서 만든
**개선된 소스코드를 커뮤니티에 기여**한다.

가장 중요한 원칙 한 줄: **저자의 라벨·주석을 소비하지 말고, 처음부터 다시 도출해 검증하라
(Reproduce, don't consume).**

---

## 조문 (Articles)

### 제1조 — 독립 재현, 소비 금지 (Reproduce, don't consume)
원 논문의 분석을 **우리가 새로 작성한 GPU 중심 Python 코드로 처음부터 다시 도출**한다.
저자가 제공한 처리·주석 결과(세포유형 라벨, 악성세포 판정, 클러스터, 임베딩)를 **분석의
입력으로 소비하지 않는다** — 그것들은 우리가 독립적으로 재현·검증해야 할 저자의 **주장
(claim)** 이다. 공개 데이터가 처리 완료 행렬뿐이어도, 그 행렬에서 QC·클러스터링·세포유형
주석·악성세포 식별을 우리 코드로 다시 수행하고 저자 라벨은 **오직 검증 대조용으로만** 쓴다.

### 제2조 — 독립 검증 (Independent validation)
우리의 독립 결과를 저자 주장과 **정량적으로 대조**한다(예: ARI, marker 중복, 세포유형·조성
일치, CNV 농도). **일치·부분일치·불일치**를 정직하게 판정해 보고하고, 원 논문 결론을
지지하는지 분명히 밝힌다.

### 제3조 — 공개 원자료 필수 (Public raw data only)
저자가 실제로 사용한 **원본 데이터가 공개**(GEO/SRA/ENA/Zenodo 등)된 논문만 대상으로 한다.
접근번호(accession)는 **실제 존재를 확인**하고, 불확실하면 "확인 필요"로 표기하며 **절대
지어내지 않는다**.

### 제4조 — 현대 스택 (Modern GPU-Python stack)
구식 R/Seurat 워크플로를 **Python(Scanpy)·GPU(scVI·rapids-singlecell·PyTorch)·AI 해석**으로
대체해, 같은 결론에 **더 빠르고 재현 가능하게** 도달하는지 보인다.

### 제5조 — 커뮤니티 기여 (Contribute upgraded code)
새로 작성한 **재사용 가능한 GPU 중심 소스코드**를 커뮤니티에 개선된 형태로 남긴다. 다른
암+scRNA-seq 연구자가 그대로 가져다 쓸 수 있어야 한다.

### 제6조 — 정직 (Honesty about the level of reproduction)
"재현"의 **층위를 구분**한다 — ① 원시 리드(FASTQ)부터의 독립 복제, ② 저자 코드로의 정밀
재현, ③ 처리 데이터로부터의 계산 재현은 서로 다른 주장이다. 무엇을 했고 무엇은 하지
않았는지, **한계를 명시하고 과장하지 않는다**.

---

## 왜 이 헌장이 생겼나

췌장암(Peng 2019) 튜토리얼의 첫 판은 저자가 이미 붙여 둔 세포유형 라벨(`Cell_type`,
악성 도관 type2 포함)을 **그대로 입력으로 받아** 다운스트림만 검증했습니다. 결과는
맞았지만, 그것은 저자의 판단을 *전제*한 검증이지 **독립 재현이 아니었습니다**(제1조 위반).
반면 therapy-induced evolution(Maynard 2020) 튜토리얼은 저자 원자료에서 **새로 짠 GPU
Python 코드로 QC·클러스터링·세포유형 주석까지 다시 수행**하고 저자 결론과 대조했습니다 —
이것이 BioIDE가 지향하는 기본형입니다. 이 차이를 제도화하기 위해 헌장을 명문화했습니다.
