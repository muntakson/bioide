# 이 튜토리얼은 어떻게 만들어졌는가

*(How this tutorial is made — 췌장암 PDAC 파이프라인 제작·검토 기록)*

이 문서는 **"췌장암 종양 생태계 (Peng 2019, PDAC)"** 튜토리얼이 처음의 AI 초안부터
검증·배포까지 어떤 과정을 거쳤는지 기록하고, 그 과정에서 나온 **정직한 방법론적 질문**
— *"원 논문의 소스코드도 없이 어떻게 워크플로를 재현했는가?"* — 에 답합니다.
과장 없이, 무엇을 재현했고 무엇은 재현하지 **않았는지**까지 적었습니다.

---

## 1. 전체 타임라인

| 단계 | 무슨 일이 있었나 |
|------|------------------|
| ① 파이프라인 만들기 클릭 | "파이프라인 만들기" 카드가 Anthropic API를 호출 → **잔액 소진으로 실패**. |
| ② LLM 전환 | Claude 대신 **Groq / DeepSeek**로 provider를 바꾸자 카드가 정상 동작해 초안(.md + 스크립트)을 생성. |
| ③ Claude Code 검토 | 생성된 초안을 검토하니 **환각(hallucination)투성이**였음(아래 §3). |
| ④ 데이터 검증 | 인용된 데이터셋(accession)이 실제로 존재·접근 가능한지 확인 → **저자 데이터셋 인용 자체가 틀림**을 발견. |
| ⑤ 재작성 | 실제 공개 데이터(Peng 2019 PDAC)로 파이프라인 전체를 **행렬 기반**으로 다시 작성. |
| ⑥ 실행 검증 | 스텝 1~3을 **실제 데이터로 end-to-end 실행**해 논문 핵심 결과 재현을 확인. |
| ⑦ 리포트 | 전문가용 PDF 리포트(6쪽)를 빌드해 육안 검증. |
| ⑧ 승격·배포 | `modules/`로 승격, 버전 0.9.4→0.9.5, `build.sh`로 code-server에 배포. |
| ⑨ API 재충전 확인 | Anthropic 잔액 재충전 후, "학생용 논문작성" 버튼과 **똑같은 호출 경로**로 키가 동작함을 실호출로 확인(HTTP 200, 스트리밍 정상). |
| ⑩ 논문 생성 | "학생용 논문작성" 클릭 → 정상 생성. |

이 과정에서 흥미로운 지점은 **AI가 만든 초안을 그대로 믿지 않고, 사실·데이터·실행을
사람(과 Claude Code)이 한 겹씩 검증**했다는 것입니다. AI 초안은 *출발점*이지 *결과물*이
아니었습니다.

---

## 2. 핵심 질문 — 소스코드 없이 어떻게 재현했나?

> **짧은 답:** 원 논문의 분석 소프트웨어를 처음부터 다시 짜지 **않았습니다**. 원시 시퀀싱
> 리드(FASTQ)에서부터 저자의 파이프라인을 똑같이 돌린 것도 **아닙니다**. 대신
> **저자가 이미 공개해 둔, 처리·주석이 끝난 데이터**를 받아, **표준 오픈소스 도구
> (Scanpy)** 로 논문의 핵심 그림·주장을 **다시 계산**해 일치하는지 확인했습니다.

이걸 정확히 이해하려면 "재현(reproduction)"에 세 가지 층위가 있음을 구분해야 합니다.

### 재현의 세 층위

1. **독립 복제 (independent replication) — 원시 리드부터 새로 처리**
   저자의 FASTQ를 받아 *내* 방식(정렬·QC·클러스터링·주석)으로 처음부터 분석해 결론이
   재현되는지 보는 것. → **하지 않았습니다.** 이 박스는 aarch64라 Cell Ranger가 없고,
   10x 원시 데이터 수십 GB를 정렬하는 것도 비현실적입니다. (초안이 시도한 STARsolo/BAM→FASTQ
   경로가 바로 이것인데, 환각인 데다 이 환경에선 애초에 불가능했습니다.)

2. **파이프라인 정밀 재현 (exact-pipeline reproduction) — 저자 코드·파라미터로 FASTQ부터**
   저자의 소스코드와 정확한 파라미터를 구해 그들의 파이프라인을 그대로 재실행하는 것.
   → **이건 정말로 저자 소스코드가 필요합니다.** 하지 않았습니다.

3. **처리 데이터로부터의 계산 재현 (computational reproduction from processed data)**
   저자가 공개한 **이미 처리·주석된 세포 행렬**을 받아, **표준·독립 도구**로 논문의 핵심
   결과(세포 아틀라스, 악성 도관세포 marker, 종양 특이성)를 **다시 계산**해 논문 주장과
   맞는지 검증하는 것. → **이것이 이 튜토리얼이 한 일입니다.**

### 왜 소스코드가 필요 없었나 — 라벨이 데이터 안에 들어 있다

핵심은 이것입니다. 저자가 Zenodo에 올린 `.h5ad`(AnnData) 객체 안에는 **세포×유전자 발현
행렬뿐 아니라, 저자가 직접 매긴 세포유형 라벨(`Cell_type`)** 이 함께 들어 있습니다.
여기에는 논문의 핵심인 **`Ductal cell type 1`(정상 도관, 10,317세포)와
`Ductal cell type 2`(악성 도관, 11,307세포)** 구분이 그대로 보존돼 있습니다.

즉, **"어떤 세포가 악성인가"라는 저자의 판단(=그들의 분석 코드가 낸 결론)이 데이터
파일에 이미 결과로 담겨 있으므로**, 그 판단을 재현하기 위해 저자의 코드가 필요하지
않았습니다. 우리는 그 라벨을 **입력으로 받아들이고**, "그렇다면 악성 도관세포는 정상
도관세포와 어떻게 다른가?"를 표준 통계로 **다시 계산**했습니다.

### 어떤 도구로 다시 계산했나 — 저자 코드가 아니라 커뮤니티 표준

분석에 쓴 것은 저자의 소프트웨어가 아니라, 단일세포 분야의 **표준 오픈소스 스택**입니다:

- **Scanpy / AnnData** — 널리 쓰이는, 독립적으로 구현된 scRNA-seq 분석 라이브러리.
- 표준 알고리즘: 정규화·로그변환, PCA/UMAP 임베딩, **Wilcoxon rank-sum 검정**(세포유형별
  marker), **`score_genes`**(악성 서명 점수). 이들은 특정 논문의 사유 코드가 아니라
  **분야 공용 방법**입니다.

그래서 우리가 그린 그림들 — 10개 세포유형 UMAP, 악성 vs 정상 도관 DE marker, 악성 서명
점수 분포 — 은 **저자 코드를 재실행한 것이 아니라, 저자의 세포·라벨을 표준 도구로 독립적으로
재분석한 결과**입니다. 그리고 그 결과가 논문의 주장과 맞았습니다:

- 악성 도관세포 marker: **S100A6·MUC1·KRT19·S100P·LGALS3** (전형적 PDAC 종양세포 유전자)
- 악성 서명 점수: **악성 0.93 vs 정상 0.025** (뚜렷한 분리)
- **종양 조직 도관세포의 ~81%가 악성 vs 정상 조직 ~0%** (악성 도관 type2의 종양 특이성)

이것이 논문의 중심 발견 — *정상 도관과 구별되는 악성 도관 집단이 종양에서 나타나며 종양 내
이질성을 이끈다* — 와 일치합니다.

---

## 3. 처음 AI 초안의 환각들 (교체 전)

"파이프라인 만들기"가 Groq/DeepSeek로 만든 첫 초안은 그럴듯했지만 사실이 대거 틀렸습니다:

- **잘못된 논문 인용:** system 프롬프트가 *"Puram et al. (2017)이 췌장암에서…"* 라고 했으나
  Puram 2017은 **두경부암(HNSCC)** 논문입니다.
- **잘못된 데이터셋:** `GSE81903`을 췌장암 BAM 다운로드로 지목 — 실제와 불일치.
- **잘못된 규모:** *"160개의 샘플"* — 환각.
- **돌아가지 않는 코드:** 존재하지 않는 `pysam.fastq()` 호출, `--genomeGenerate`와
  `--readFilesIn`을 한 명령에 섞은 잘못된 STARsolo 문법, DataFrame을 scVI에 그대로 전달 등
  — 실행하면 크래시.
- **끊긴 의존성:** Step 4가 `kind:"ai"`인데 `run`이 비어 `processed_data.csv`를 아무도
  안 만드는데 Step 5·6이 그걸 참조.
- **매니페스트 불일치:** `easyReport.makeReport`가 존재하지 않는 파일을 가리킴.

이 전부를 버리고, 검증된 실제 데이터로 melanoma-tirosh 예제와 같은 **행렬 기반** 구조로
다시 썼습니다.

---

## 4. 발견한 데이터 결함과 그 해결

- **원시 데이터의 위치 문제:** Peng 2019의 원시 데이터는 중국 GSA(`CRA001160` /
  BioProject `PRJCA001063`)에 있어 다운로드가 번거롭습니다.
- **해결:** 저자 데이터를 **BESCA 워크플로로 재처리·주석한 공개본**이 Zenodo(record
  `3969339`, `...besca2.annotated.h5ad`, ~1.68 GB)에 있습니다. HEAD 요청으로 **실제 존재
  (HTTP 200, 1,681,192,568 bytes)** 를 확인한 뒤 이 파일을 사용했습니다.
- **주석 라벨의 함정:** 그 재처리본의 besca 라벨(`celltype1`/`celltype3`/`dblabel`)은 도관
  세포를 **하나("pancreatic ductal cell")로 뭉쳐** type1/type2를 나누지 않습니다. 처음
  실행에서 스크립트가 이 뭉친 라벨을 골랐습니다.
- **해결:** 같은 객체의 **`Cell_type` 컬럼이 저자 원본 10-세포유형 주석**이며 여기에
  type1/type2 구분이 그대로 있음을 발견하고, 세포유형 컬럼 감지 우선순위를 `Cell_type`으로
  바꿔 **논문의 악성/정상 도관 구분을 직접 재현**했습니다.

### 실행하며 잡은 실제 버그 3가지
1. **세포유형 컬럼 선택** — besca의 뭉친 라벨 대신 저자 원본 `Cell_type` 우선.
2. **조건 정규화** — `Type` 컬럼의 `T`/`N`이 `Tumor`/`Normal`로 매핑되지 않아 리포트
   표지에 종양/정상 세포 수가 0으로 찍히던 버그 수정.
3. **한글 폰트** — matplotlib 기본 폰트(DejaVu)에 한글이 없어 그림 제목이 □로 깨지던 것을
   Noto CJK로 수정.

---

## 5. 무엇을 재현했고, 무엇은 재현하지 않았나 (한계)

**재현한 것 ✅**
- 저자의 세포·라벨을 표준 도구로 재분석해 **세포 아틀라스**와 **악성 도관세포의 특징·종양
  특이성**이라는 논문의 핵심 결론이 재현됨을 확인.

**재현하지 않은 것 / 주의할 점 ⚠**
- **원시 리드(FASTQ)에서부터의 독립 복제 아님.** 저자의 정렬·QC·정규화 결과를 그대로
  물려받았습니다. 그 처리 단계에 오류가 있다면 우리 결과도 그 영향을 받습니다.
- **세포유형을 독립적으로 재발견한 것이 아님.** 악성/정상 도관 라벨은 **저자의 판단**이며,
  우리는 그 라벨을 *전제로* 다운스트림 주장을 검증했습니다. (라벨 자체의 타당성을 독립적으로
  증명한 것은 아닙니다.)
- **besca 재처리본**의 세부 수치는 논문 원본과 미세하게 다를 수 있습니다(유전자 2,033개로
  축소된 HVG 객체).

이 구분은 중요합니다. 이 튜토리얼은 **"저자의 데이터·주석이 주어졌을 때 그들의 다운스트림
결론이 표준 도구로 재현되는가"** 를 보여주는 교육용 재현이지, 원 논문에 대한 독립적 반증·검증
연구가 아닙니다.

---

## 6. 리뷰 포인트 (읽으며 곱씹을 것)

- **AI 생성물은 초안이지 결과가 아니다.** 인용된 accession·논문·파라미터는 반드시 실제
  존재·접근성을 확인해야 합니다. 첫 초안은 데이터셋 인용부터 틀렸습니다.
- **"재현했다"는 말의 층위를 분명히 하라.** 처리 데이터로부터의 계산 재현과, 원시 리드부터의
  독립 복제는 전혀 다른 주장입니다. 과장하지 않는 것이 과학적 정직입니다.
- **공개 데이터는 종종 결론(라벨)을 데이터 안에 담고 있다.** 그래서 저자 코드 없이도 다운스트림
  주장을 검증할 수 있지만, 동시에 그 라벨에 의존한다는 한계도 생깁니다.
- **환경 제약이 방법을 결정한다.** aarch64·Cell Ranger 부재 때문에 행렬 기반 재현을 택했고,
  이것이 오히려 더 정직하고 재현 가능한 튜토리얼이 됐습니다.
- **LLM은 갈아끼울 수 있다.** Anthropic 잔액이 없을 때 Groq/DeepSeek로 초안을 만들고, 재충전
  후 Anthropic으로 논문을 생성했습니다. provider 전환이 파이프라인을 막지 않습니다.

---

## 부록 — 재현에 사용한 실제 사양

- **논문:** Peng et al., *Cell Research* 2019, "Single-cell RNA-seq highlights intra-tumoral
  heterogeneity and malignant progression in pancreatic ductal adenocarcinoma" — 24 primary
  PDAC + 11 control, 57,530 cells.
- **데이터:** Zenodo `3969339` (`StdWf1_PRJCA001063_CRC_besca2.annotated.h5ad`, ~1.68 GB) /
  BioProject `PRJCA001063` / GSA `CRA001160`. 주석 라벨은 obs `Cell_type`.
- **도구:** Scanpy / AnnData (오픈소스), Wilcoxon rank_genes_groups, score_genes, UMAP.
- **파이프라인 파일:** `modules/scrna-seq/pipelines/pancreatic-cancer-sc-rna-seq/`
  (`00_setup_env.sh`, `01_download_peng_pdac.sh`, `02_figure1_atlas.py`,
  `03_figure2_malignant_ductal.py`, `05_make_report.sh`, `05_make_easy_report.sh`).
- **검증:** 스텝 1~3 end-to-end 실행(exit 0), 전문가 리포트 PDF 6쪽 빌드, API 스트리밍 실호출
  확인(claude-opus-4-8, HTTP 200).
